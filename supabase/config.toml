# Auth deep-link config for device testing. Only override fields we need;
# other auth settings use Supabase CLI defaults — verify after push.
project_id = "sdyfwnpzvlicgwcvksjh"

[auth]
site_url = "milestones://"
additional_redirect_urls = [
  "milestones://**",
  "milestones://",
]

[auth.email]
enable_confirmations = true
max_frequency = "1m0s"
otp_length = 8

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true
